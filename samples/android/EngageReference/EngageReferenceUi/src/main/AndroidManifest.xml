<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.rallytac.engagereference.ui">

    <!-- Ignore Android Studio complaining about this permission for system apps -->
    <uses-permission android:name="android.permission.BATTERY_STATS"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <!-- The Core will launch this activity at startup.  This meta-data MUST be defined! -->
        <meta-data
            android:name="com.rallytac.engagereference.core.LAUNCH_ACTIVITY"
            android:value="com.rallytac.engagereference.ui.LcarsActivity" /> <!-- NOTE: Provide your Google API key for Google Maps. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".DeveloperTestActivity">

        </activity>

        <activity
            android:name=".LcarsActivity"
            android:launchMode="singleInstance"
            android:showOnLockScreen="true"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter android:priority="2147483647">
                <action android:name="android.speech.action.WEB_SEARCH" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter android:priority="2147483647">
                <action android:name="android.speech.action.VOICE_SEARCH_HANDS_FREE" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.sec.action.SVOICE" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>